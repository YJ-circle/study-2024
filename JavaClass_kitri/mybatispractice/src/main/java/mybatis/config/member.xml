<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="member">

	<select id="findMemberId" resultType="com.spring.springorm.mybatis.mybatispractice.dao.Member">
		SELECT id, password, name, address
		FROM member
		WHERE id=#{value}
	</select>
	
	<select id="findMemberAll" resultType="com.spring.springorm.mybatis.mybatispractice.dao.Member">
		SELECT id, password, name, address
		FROM member
	</select>
	
	<insert id="addMember" parameterType="com.spring.springorm.mybatis.mybatispractice.dao.Member">
		INSERT INTO "SPRINGJDBC"."MEMBER" (ID, PASSWORD, NAME, ADDRESS) VALUES (#{id}, #{password}, #{name}, #{address})
	</insert> 
	
	<update id="updateMember" parameterType="com.spring.springorm.mybatis.mybatispractice.dao.Member">
	</update>
</mapper>
